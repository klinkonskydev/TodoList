import styled from "styled-components";

const Container = styled.div`
    width: 100%;
    height: 70vh;

    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;

    font: 300 26px var(--font-family);
    cursor: default;

    p
    {
        cursor: default;
    }

    p
    {
        font: 300 15px var(--font-family);
    };
`;

export { Container };